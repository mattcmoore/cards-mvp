const ColorPicker = ({colors}) => { 
    return(
        <div className="color-picker">
   `         {/* {colors.map( (color) =>(
                <div className="color-dot" style={color}></div>
            ))}     `      */}
        </div>
    )
}

export default ColorPicker